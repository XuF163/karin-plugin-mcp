/**
 * Command apps entrypoint.
 *
 * This plugin mainly exposes an HTTP bridge (`/MCP`) and a stdio MCP server.
 * Keeping this file ensures `karin.ts-apps` / `karin.apps` directories exist.
 */

export {}

